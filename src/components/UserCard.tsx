import React from 'react';
import { Card, Avatar } from 'antd';

const UserCard = (props:any) => {
  return (
    <div>
      <p>Contentt</p>
      <p>{props.id}</p>
    </div>
  )
}

export default UserCard;